<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.william.speechpad_v0.item_ListViewCategoria"
    android:id="@+id/item_ListViewCategoria"
    android:background="#ffffff"
    android:backgroundTintMode="multiply"
    android:focusable="false"
    android:elevation="@dimen/abc_panel_menu_list_width"
    style="@android:style/Theme.Panel">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@android:style/TextAppearance.Material.Headline"
        android:text="Categoria"
        android:id="@+id/txtNomeCategoria"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:textColor="#455A64"
        android:textStyle="bold|normal" />

    <ImageButton
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="Excluir"
        android:id="@+id/btnExcluirCategoria"
        android:textColor="#ffffffff"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:textStyle="bold"
        android:src="@drawable/ic_action_delete"
        android:layout_alignBottom="@+id/lblQtdLembretesSalvos"
        android:tint="@color/ColorImageButton"
        android:background="?attr/selectableItemBackground"
        android:tintMode="src_in"
        android:elevation="@dimen/abc_action_bar_default_height_material" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="Quantidade de Lembretes Salvos"
        android:id="@+id/lblQtdLembretesSalvos"
        android:layout_below="@+id/txtNomeCategoria"
        android:layout_alignParentStart="true" />
</RelativeLayout>
